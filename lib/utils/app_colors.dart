import 'package:flutter/material.dart';

class AppColors {
  // Base Colors
  static const Color background = Color(0xFF0F172A); // Deep Void
  static const Color backgroundLight = Color(0xFF1E293B);

  // Accents
  static const Color primary = Color(0xFFFF007F); // Neon Pink / Fuchsia
  static const Color secondary = Color(0xFF9D4EDD); // Soft Purple
  static const Color accent = Color(0xFFFF007F);

  // Glass Effects
  static const Color glassBorder = Color(
    0x1AFFFFFF,
  ); // Frosted white low opacity
  static const Color glassBackground = Color(
    0x1AFFFFFF,
  ); // 10% opacity white-ish

  // Text
  static const Color textPrimary = Color(0xFFF1F5F9);
  static const Color textSecondary = Color(0xFF94A3B8);
}
